import React from 'react';
import { Paper, CircularProgress } from '@mui/material';

const Loading = () => {
    return (   
        <>
            <CircularProgress />
        </>
    );
}

export default Loading;